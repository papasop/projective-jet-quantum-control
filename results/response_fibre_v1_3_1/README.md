# v1.3.1 recorded run

`full_run.log` is the complete user-supplied textual output of the full
ten-segment v1.3.1 execution.

`report.json` is the final report object extracted from that log without
changing numeric values.

The SHA-256 identifier recorded as `certificate_sha256` refers to the
certificate generated by the original execution. The certificate JSON itself
is not included in this update package because it was not supplied with the
run log.

## Interpretation correction

The aggregate status is INCONCLUSIVE solely because uniform finite-error
descent over the declared common window closes for only 2/10 endpoint steps.
The connected-curve, shared-endpoint, endpoint-root, and stepwise L6 gates all
pass.
